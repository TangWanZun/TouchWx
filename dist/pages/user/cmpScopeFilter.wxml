<view class='page-body'>
    <!-- <checkbox-group >
        <label class='item' wx:for="{{formList.G0.list}}" wx:key="{{item.CmpCode}}" data-index="{{index}}" data-isauth="{{item.IsAuth}}" bindtap='changeCheck'>
            <checkbox value="{{index}}" checked="{{item.IsAuth}}" />{{item.CmpName}}
        </label>
    </checkbox-group> -->
    <view class="item" wx:for="{{formList}}" wx:key="{{item.CmpCode}}">
        <label class='item-con'>
            <checkbox  />{{item.CmpName}}
        </label>
        <view class="item" wx:for="{{item.list}}" wx:for-item="typeItem" wx:for-index="typeIndex" wx:key="{{typeIndex}}">
            <label class='item-con'>
                <checkbox  />{{typeIndex}}
            </label>
            <view class='item' wx:for="{{typeItem}}" wx:for-item="childItem" wx:for-index="childIndex" wx:key="{{childItem.CmpCode}}">
                <label class='item-con'>
                    <checkbox  />{{childItem.CmpName}}
                </label>
            </view>
        </view>
    </view>
    <view class='button'>
        <!-- <button style='margin-bottom:10px;' bindtap='reset'>重置</button> -->
        <button type="primary" bindtap='submit'>保存</button>
    </view>
</view>