.page-list-loging{
  color: rgba(0, 0, 0, 0.3);
  font-size: 12px;
  padding:20px 0;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: nowrap;
  position: relative;
}
.page-list-loging>view{
  flex-shrink: 0;
  color: rgba(0, 0, 0, 0.3);
}
@keyframes appListLoging{
  0%{
    transform: rotate(0deg)
  }
  100%{
    transform: rotate(359deg)
  }
}
.page-list-loging>image{
  margin-left: 5px;
  width:15px;
  height: 15px;
  animation: appListLoging 1s;
  animation-iteration-count:infinite;
  animation-timing-function:linear;
  opacity: 0.5;
}
/* 暂无数据 */
.page-noneData{
  text-align: center;
  color: rgba(0,0,0,0.5);
  font-size: 17px;
  padding:10px;
}